{{--Contributor or Supervisor--}}
<div class="well">
    <h2>
        ยินดีต้อนรับสู่ระบบบริหารจัดการ Living University
        <div class="small" style="margin-top: 10px;">ผู้ใช้ระบบ : {{{ Auth::user()->getFullName() }}} ({{{ Auth::user()->roleDisplay() }}})</div>
    </h2>
    <div class="clearfix"></div>
</div>